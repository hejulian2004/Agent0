# requirements.txt records the full set of dependencies for development

# Core ML frameworks
# NOTE: vLLM 0.8.2 (used for rollout/eval) requires torch 2.6.x; install torch
# to match your vLLM build (see README). transformers >= 4.49 is required for
# the Qwen2.5-VL (Qwen2_5_VL) model classes used throughout.
torch>=2.4.0
transformers>=4.49.0
accelerate>=0.25.0
datasets>=2.16.0

# VERL dependencies
ray[default]>=2.9.0
tensordict<=0.6.2
flash-attn>=2.5.0
liger-kernel

# Vision-Language Model support
qwen-vl-utils>=0.0.2

# vLLM for efficient inference (required for RL rollout and evaluation;
# commented out so CPU-only dev installs succeed — install on GPU machines).
# vllm 0.8.2 pulls in torch 2.6.x; keep torch aligned with the vLLM build.
# vllm==0.8.2

# HTTP sandbox client (only needed when SANDBOX_ENDPOINT is used;
# the default subprocess sandbox has no extra dependencies)
aiohttp>=3.9.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0
pyarrow>=15.0.0
Pillow>=10.0.0
opencv-python>=4.8.0

# Tool execution
timeout-decorator>=0.5.0
autopep8>=2.0.0
sympy>=1.12

# OCR and detection (optional)
# easyocr>=1.7.0
# paddlepaddle>=2.5.0
# paddleocr>=2.7.0
# ultralytics>=8.0.0

# Vision-language SFT (paper pipeline, scripts/sft_stage1.sh / sft_stage2.sh)
# ms-swift>=3.0.0

# Configuration and logging
hydra-core>=1.3.0
omegaconf>=2.3.0
wandb>=0.16.0
tqdm>=4.66.0

# Code quality
peft>=0.7.0
pybind11>=2.11.0
pylatexenc>=2.10
pylint==3.3.6

# Evaluation
math-verify>=0.3.0

# Development
codetiming
dill
torchdata
pytest>=7.4.0
pytest-cov>=4.1.0